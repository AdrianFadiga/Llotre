import { Response } from 'express';
import IRequestWithAdmin from '../interfaces/IRequestWithAdmin';

export default {
    async getUserTasks(req: IRequestWithAdmin, res: Response) {
        const { admin } = id;
    }
};